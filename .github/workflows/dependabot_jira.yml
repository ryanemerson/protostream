name: Dependabot Jira

on:
  pull_request:
    branches:
      - 4.6.x
      - main

    types:
      - opened
      - reopened

jobs:
  summary:
#    if: github.actor == 'dependabot[bot]'
    runs-on: ubuntu-latest

    outputs:
      summary: ${{ steps.summary.outputs.summary }}

    steps:
      - id: summary
        run: echo summary=$(echo ${{ github.event.pull_request.title }} | grep -oP '(?<=Bump )(.*)(?=from)|(?<=to )(.*)' | tr -d '\n') >> $GITHUB_OUTPUT

  jira:
    needs: summary
    uses: ryanemerson/infinispan/.github/workflows/jira_upsert.yml@dependabot_jira_as_workflow
    secrets:
      token: ${{ secrets.JIRA_API_TOKEN }}
    with:
      infinispanBranch: dependabot_jira_as_workflow # TODO change to main
      project: IPROTO
      summary: ${{ needs.summary.outputs.summary }}
      type: Component Upgrade
      pullRequest: ${{ github.event.pull_request.html_url }}

  comment:
    needs: jira
    runs-on: ubuntu-latest
    permissions:
      pull-requests: write
    steps:
      - name: Add comment to PR
        uses: actions/github-script@v7
        with:
          script: |
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: '${{ needs.jira.outputs.url }}'
            })
